<div class="text-center h3 mb-3 mt-3">
  新規ユーザー登録
</div>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="row">
    <div class="col-12 col-lg-8 offset-lg-2">
      <div class="field form-group">
        <%= f.label :email, 'メールアドレス', class: "col-form-label" %><br>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", size: 40, class: "form-control" %>
      </div>

      <div class="field form-group">
        <%= f.label :password, class: "col-form-label" %><br>
        <%= f.password_field :password, autocomplete: "off", size: 40, class: "form-control" %>
      </div>

      <div class="field form-group">
        <%= f.label :password_confirmation, class: "col-form-label" %><br>
        <%= f.password_field :password_confirmation, autocomplete: "off", size: 40, class: "form-control" %>
      </div>
      <div class="actions text-center">
        <%= f.submit "登録", class: "btn btn-info" %>
      </div>
    </div>
  </div>
<% end %>